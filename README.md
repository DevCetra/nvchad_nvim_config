# Config for NvChad/NeoVim (MacOS)

## Setup

1. Install NeoVim, NvChad, Dart, Flutter, Rust, Node, git, etc.
2. Copy contents of repo to `~/.config/nvim/`.

## Copilot Setup

Authenticate via `:Copilot auth`
